html ,
body {
  @apply text-black font-sans;
  background: #f4f4f4;
}

a,
a:active,
a:focus,
a:visited {
  @apply no-underline;
  color: inherit;
}

article {
  &:not(.index-1) {
    @apply mt-8 pt-8 border-t border-black;
  }
}
