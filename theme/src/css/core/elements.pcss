html ,
body {
  @apply text-black bg-white font-sans;
}

a,
a:active,
a:focus,
a:visited {
  @apply no-underline;
  color: inherit;
}

article {
  &:not(.index-1) {
    @apply mt-8 pt-8 border-t border-black;
  }
}
